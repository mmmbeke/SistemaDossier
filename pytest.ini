[pytest]
testpaths = tests
pythonpath = src
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: requiere PostgreSQL y JWT_SECRET en .env
    security: pruebas de autorización y tokens
    slow: pruebas lentas o que llaman servicios externos
