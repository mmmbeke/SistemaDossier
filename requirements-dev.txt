# Dependencias de desarrollo y pruebas (pip install -r requirements-dev.txt)
-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
pytest-cov>=6.0.0

# Seguridad (SAST + auditoría de dependencias)
bandit>=1.8.0
pip-audit>=2.7.0

# Rendimiento / estrés (opcional)
locust>=2.32.0

# Contrato OpenAPI (fuzzing)
schemathesis>=3.38.0
