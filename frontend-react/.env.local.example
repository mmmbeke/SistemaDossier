# Copia este archivo a `.env.local` en esta carpeta (`frontend-react/`) y ajusta la URL.
# Next.js solo expone al navegador variables que empiezan por NEXT_PUBLIC_.

NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
